# DEVELOPMENT_JOURNEY

> 개발 일지. 사실·정량 로그만 담는다. 홍보/어필 서술은 [README](README.md), 상세 지표 표는 [EVALUATION](EVALUATION.md).

## 상태표 (최신 우선)

| 날짜(ISO) | 분류 | 내용 | 관련ID |
|---|---|---|---|
| 2026-09-18 | result | NLI 충실성 축(51문항): 환각율 oracle 0.222 → 검색 근거 0.385, 충실율 0.778→0.615. 검색 노이즈가 정확도·충실성 둘 다 깎음 | B-9 |
| 2026-09-18 | result | W2 답변 하네스 착수(51문항): Answer-F1 추출 베이스라인 0.137 → 7B 생성(oracle) 0.533 → 검색 근거 0.416, 검색 오류 손실 0.116 | B-8 |
| 2026-09-18 | decision | 7B 양자화 = bitsandbytes 4-bit. AWQ는 autoawq가 torch 2.3.1 강제해 cu121 파손 → 폴백 | B-8 |
| 2026-09-18 | result | 파이프라인 비교(10편 26문항·후보20): dense→리랭커 Hit@5 0.692 > dense 0.615 > BM25→리랭커 0.462, 1단계 검색 recall이 리랭커 천장 | B-7 |
| 2026-09-18 | result | dense(bge-m3)·BM25·hybrid(RRF) 검색 비교(10편 26문항): Hit@5 dense 0.615 > hybrid 0.423 > BM25 0.385, 단순 RRF는 이득 없음 | B-6 |
| 2026-09-18 | result | 리랭커(bge-reranker-v2-m3) 후보20 재정렬: 평균 Hit@5 0→0.25·Recall 0→0.25·MRR 0→0.125, 회복 가능 3개 중 1개 회복 | B-5 |
| 2026-09-18 | result | 같은 4개 질문에서 k=5→10: 평균 Hit 0→0.5, Recall 0→0.375, MRR 0→0.069444 | B-4 |
| 2026-09-18 | feat | BM25 평가 루프 구현: 9개 중 4개 평가·5개 보류, Hit@5·Recall@5·MRR@5 모두 0 | B-3 |
| 2026-09-10 | feat | BM25 문단 검색 구현, 예제 질문 정답 근거 11위 확인 | B-2 |
| 2026-09-10 | feat | 단일 논문 로더 구현, 단위 테스트 4개 통과 및 train 샘플 근거 매핑 확인 | B-1 |
| 2026-09-10 | decision | 단일 논문 QA 완성 후 관련 논문 검색으로 확장 | P0-7 |
| 2026-09-08 | decision | P0-3 개정 — 채점 무료 로컬/Qasper-F1/NLI, Claude 는 비교·검증용(하드 의존 아님), 생성 로컬7B↔Claude ablation축 | P0-3 |
| 2026-09-08 | decision | 스택·평가셋·주차계획 확정, 리포 스캐폴딩 | P0-1 ~ P0-6 |
| 2026-09-08 | docs | DEVELOPMENT_JOURNEY / README / EVALUATION 3분리 골격 생성 | — |

---

# Part 0 — 기획 · 기술 결정

## 프로젝트 개요

**평가 주도 RAG 어시스턴트 (도메인: AI/ML 논문 / arXiv·NLP)**

대부분의 RAG 데모가 빠뜨리는 **평가 하네스**를 1급 시민으로 삼는 프로젝트. "동작하는 데모"가 아니라 **측정 → 진단 → ablation → 회귀 테스트** 루프로 검색·생성 품질을 정량 관리하는 것이 목표. 직전 프로젝트(saturi-translator)에서 보인 "데이터로 진단·held-out 측정·ablation 비교" 강점을 LLM 애플리케이션 축으로 확장.

## 기술 결정 (트레이드오프·기각 대안 명시)

### P0-1. 언어·런타임 = Python
- **결정:** Python. 로컬 GPU 임베딩/리랭커 생태계(sentence-transformers, FlagEmbedding)와 RAG 툴링이 Python 중심.
- **기각:** TS/Node — 로컬 임베딩 추론 생태계 빈약.

### P0-2. 임베딩·리랭커 = 로컬 GPU (RTX 3080, 10GB)
- **결정:** 임베딩 `BAAI/bge-m3`, 리랭커 `BAAI/bge-reranker-v2-m3` 를 로컬 GPU에서 추론.
- **근거:** (1) 비용 0 — 관리형 임베딩 API의 반복 인덱싱·재실험 비용 제거. (2) GPU 활용·온디바이스 추론 역량 어필(saturi 연장선). (3) bge-m3는 다국어·긴 컨텍스트(8k) 지원으로 논문 문단에 적합.
- **제약/미지수:** 10GB VRAM — bge-m3(560M) + 리랭커 동시 상주 시 배치 크기 조정 필요. full-text 임베딩 시 인덱싱 시간 측정 예정.
- **기각:** Voyage/OpenAI 임베딩 — 실험 반복마다 과금, 포트폴리오상 "로컬 ML" 어필 손실.

### P0-3. 채점은 무료 로컬/표준지표, Claude 는 하드 의존이 아니라 비교·검증용 (2026-09-08 개정)
- **초기 가정(폐기):** 생성·채점을 모두 Claude API 로. → 유일 과금이 채점(LLM-judge)에 몰려 예산 리스크. 또한 LLM-judge 는 채점 기준 자체라 재현성·객관성 약점.
- **개정 결정:**
  - **정량 채점 백본 = 전부 무료·재현 가능.** 검색은 gold evidence 자동채점(recall@k/MRR/nDCG). 답변 정확도는 **Qasper 공식 Answer-F1**(이 벤치마크의 표준 지표). Faithfulness/환각은 **로컬 NLI 모델**(evidence ⊨ answer 함의)로 근사.
  - **답변 생성 = 로컬 7B + Claude 를 ablation 축으로.** `Qwen2.5-7B-Instruct`(4-bit, 10GB 상주) vs `claude-sonnet-5` vs `claude-opus-5`. 생성 모델 품질을 측정 대상으로 삼음(컨셉 정합).
  - **LLM-as-judge = 선택적 검증 도구.** 소량만 돌려 "judge 점수 vs F1/NLI 상관" 을 보여 judge 신뢰도 검증. Claude Haiku + Batch API 로 ~$10–20. 미실행해도 프로젝트 성립.
- **근거:** (1) 정량 백본이 무료라 예산 리스크 제거·완전 로컬 재현 가능(포트폴리오 강점). (2) Qasper 는 원래 F1 채점 벤치마크 → 표준 지표가 임시 LLM-judge보다 객관적. (3) Claude 를 "프리미엄 비교 대상"으로 두면 로컬 vs API 트레이드오프를 **측정으로** 보여줄 수 있음.
- **기각:** 생성·채점 전면 Claude — 예산 리스크 + 채점 객관성 약화. / 채점 전면 로컬 LLM-judge — 7B judge 신뢰도 부족, 표준 F1 대비 이점 없음.
- **미지수:** 로컬 NLI 모델 선정(예: DeBERTa-v3 MNLI/ANLI)·faithfulness 근사 정확도, Qasper unanswerable/abstractive 답변의 F1 처리, 7B 생성 VRAM(임베딩·리랭커와 동시 상주 시 스케줄링).

### P0-4. 벡터 스토어 = FAISS (로컬), pgvector 는 ablation 축
- **결정:** FAISS(flat/IVF)로 시작. pgvector 는 "운영형 스토어" 비교 축으로 후순위.
- **근거:** 로컬·의존성 최소·재현 쉬움. 벡터 스토어 자체는 이 프로젝트의 핵심 변수가 아님(핵심은 평가 하네스).

### P0-5. 평가셋 = Qasper 백본 + 합성 보강
- **결정:** **Qasper**(NLP 논문 QA, gold evidence paragraph + reference answer 라벨) 를 평가 백본으로. Qasper 논문 = 코퍼스, Qasper 질문 = eval셋.
- **근거:** (1) retrieval 채점을 gold evidence 로 **자동화**(recall@k/MRR/nDCG). (2) answer faithfulness/correctness 를 reference answer 로 채점. (3) 평가셋 직접 구축 노동 대폭 절감 + "평가 주도" 컨셉 정합.
- **보강:** 커버리지 부족 유형은 Claude 로 합성 QA(질문+gold passage+reference) 생성 후 일부 수동 검증.
- **미지수:** Qasper full-text 파싱 품질, unanswerable 질문 처리 방침.

### P0-6. 서빙 = FastAPI + 최소 UI (후순위)
- **결정:** 평가 하네스·수치 확보가 먼저. 서빙(스트리밍 답변+인용 표시)은 Week 4.

### P0-7. 단일 논문 QA 완성 후 관련 논문 검색으로 확장
- **결정:** 첫 버전은 미리 적재한 Qasper 논문 하나를 선택하고, 해당 논문 안에서 근거를 검색해 답하는 QA로 구현한다. 완성 후 전체 코퍼스에서 관련 논문을 찾고, 선택한 논문에 질문하는 흐름으로 확장한다.
- **첫 버전 기능:** 논문 선택, 질문 입력, 선택한 논문 내부 검색, 근거 기반 답변, 인용 원문 확인, 근거 부족 시 답변 보류, 평가 실행 및 실패 사례 확인.
- **근거:** 검색 범위를 고정하여 문단 검색 실패와 답변 생성 실패를 구분하고, Qasper의 논문별 질문·근거를 활용해 평가 조건을 명확히 한다.
- **트레이드오프:** 사용자가 질문할 논문을 먼저 선택해야 하므로 논문 발견 기능은 제공하지 못한다. 대신 검색·생성·평가의 전체 흐름을 먼저 검증한다.
- **후순위 대안:** 전체 논문 검색과 여러 논문 종합 답변을 처음부터 구현하는 방안. 논문 선택 오류, 문단 검색 오류, 서로 다른 연구 조건의 종합 오류를 함께 다뤄야 하므로 첫 버전에서 제외한다.
- **완료 기준:** 논문 선택부터 답변·인용 확인·답변 보류까지 동작하고, 고정된 평가 조건에서 검색·답변 점수와 실패 사례를 재실행할 수 있다. 실행 방법과 측정 조건을 기록한다. 수치 목표와 답변 보류 판정 방식은 기준선 측정 전에 별도로 정한다.
- **확장 조건:** 첫 버전 완료 후 관련 논문 검색을 추가한다. 여러 논문을 종합하는 답변은 별도 설계·평가가 필요한 후속 후보이며 이번 결정에 포함하지 않는다.

## 평가 지표 (설계) — 상세는 [EVALUATION](EVALUATION.md)

- **검색(retrieval):** Recall@k, MRR@k, nDCG@k, Hit@k — gold evidence 대비.
- **답변(answer):** Answer Correctness = **Qasper Answer-F1**(무료·표준). Faithfulness/Hallucination = **로컬 NLI**(evidence⊨answer, 무료). LLM-as-judge 는 이들과의 **상관 검증용 선택 지표**.
- **회귀(CI):** 고정 eval셋 + 임계값, 변경마다 재실행·표 기록.

## Ablation 축 (예정)

청킹(크기/overlap, fixed vs semantic) · 임베딩 모델 · 검색(dense vs hybrid BM25+dense) · 리랭커(none vs bge-reranker) · top-k · 프롬프트 변형 · **생성 모델(로컬 Qwen2.5-7B vs claude-sonnet-5 vs claude-opus-5)**.

## 주차별 로드맵

아래 Week 1~4는 단일 논문 QA 첫 버전 범위에 적용한다(P0-7).

- [ ] **Week 1 — 데이터 + 베이스라인 검색.** Qasper 적재·청킹·bge-m3 임베딩·FAISS 인덱스·dense 검색. **검색 평가 하네스**(recall@k/MRR/nDCG) → 첫 수치.
- [x] **W1 중간 단계 — BM25 개발용 평가.** 단일 논문에서 근거 매핑이 명확한 4개 질문의 Hit·Recall·MRR 및 보류 사유 출력(B-3). 전체 평가셋·nDCG는 후속 범위다.
- [ ] **Week 2 — 생성 + 답변 평가.** Claude 인용 기반 생성 · LLM-judge(faithfulness/correctness/hallucination). end-to-end 베이스라인 수치.
- [ ] **Week 3 — Ablation + 회귀.** 청킹·임베딩·hybrid·리랭커·top-k·프롬프트 비교표 → best config. **회귀 하네스**(임계값 + CI).
- [ ] **Week 4 — 서빙 + 마감.** FastAPI + 최소 UI(스트리밍+인용) · 데모 GIF · 문서 마감.
- [ ] **첫 버전 완료 후 — 관련 논문 검색.** 전체 코퍼스에서 관련 논문을 찾고 선택한 논문에 질문하는 흐름으로 확장. 검색 범위가 달라진 평가 결과는 첫 버전과 분리하여 기록한다(P0-7).

---

# Part A — 개요 · 설계

_(Week 1 착수 시 작성: 문제 정의 · 유저 플로우 · 파이프라인 다이어그램)_

---

# Part B — 개발 로그

## B-1. 논문 문단과 질문·정답 근거를 어떻게 연결할 것인가

첫 입력은 Hugging Face Qasper train 첫 행(`1909.00694`)을 JSON으로 저장한 논문 한 편이다. 질문 하나에 정답 주석이 여러 개 있고, 근거에는 본문 문단뿐 아니라 표·그림 참조도 포함되어 있다.

[로더](src/ingest/qasper.py)는 Python 표준 라이브러리로 본문을 평탄화하되 원문의 섹션·문단 인덱스와 텍스트를 보존한다. 문단 ID는 `논문ID:s섹션인덱스:p문단인덱스`로 만든다. 근거는 원문과 정확히 일치하는 모든 문단 ID에 연결하고, 일치하지 않는 근거도 원문을 보존한다. 질문·정답 목록 및 섹션·문단 목록 길이가 다르면 오류를 내어 조용한 데이터 누락을 막는다.

**Python 3.10.7에서 테스트 4개가 통과했다. 실제 샘플은 문단 72개, 질문 9개, 정답 주석 12개였다. 정답 주석별 근거 22개 중 본문에 연결된 텍스트 근거는 16개, 표·그림 근거는 6개, 미연결 텍스트 근거는 0개였다.** 중복 주석을 포함한 집계이며 전체 데이터셋 통계가 아니다. `30 words` 정답의 근거가 `1909.00694:s11:p4`에 연결되는 것도 확인했다.

검증 명령: `python -m unittest discover -s tests -v`, `python -m src.ingest.qasper data/qasper-train-first-paper.json`. [테스트](tests/test_qasper.py)는 중복 문단·빈 문단의 위치 보존, 미연결 근거 보존, 복수 정답, `false`와 `null` 구분, 목록 길이 불일치를 확인한다. 실행 시간·검색 및 답변 품질은 아직 측정하지 않았다.

현재 입력 범위는 Hugging Face 형식의 단일 논문 JSON이다. 전체 데이터 다운로드, 공식 배포 파일 변환, 청킹, 검색, 표·그림 평가 정책은 이번 구현에 포함하지 않았다.

원칙: 평가 근거를 정리할 때는 원문과 위치를 보존하고, 연결되지 않은 항목도 집계할 수 있게 남긴다.

---

## B-2. 문단을 그대로 검색하면 정답 근거를 찾을 수 있는가

청킹이나 임베딩 적용 전에 [BM25 검색](src/retrieve/bm25.py)을 구현했다. 검색 함수는 문단 목록과 질문 문자열만 받고 정답 주석은 받지 않는다. 기존 문단을 검색 단위로 사용하고 소문자 변환 후 `\w+`로 토큰을 추출한다. 불용어 제거·어간 추출은 적용하지 않았다. 추가 실험에 앞서 이 조건을 기준선으로 고정한다.

`rank-bm25==0.2.2`의 BM25Okapi(`k1=1.5`, `b=0.75`, `epsilon=0.25`)를 사용한다. 토큰이 없는 문단은 검색에서 제외하되 원문 ID는 유지한다. 동점은 원문 순서로 정렬하고, 질문에 본문 어휘가 전혀 없으면 빈 결과를 반환한다. 이 처리는 답변 가능 여부를 판정하는 모델을 구현한 것은 아니다.

**train 논문 `1909.00694`의 문단 72개 중 검색 가능한 문단은 56개였다. 질문 `d0bc782961567dc1dd7e074b621a6d6be44bb5b4`의 정답 근거는 11위였으며 상위 5개에 포함되지 않았다.** 1위 문단은 `used`, `training`에서 각각 약 1.6295, 1.7571의 점수를 받았지만 정답 문단에는 해당 토큰의 점수가 없었다. 단어 일치 점수가 질문의 수량 의도와 일치하지 않는 사례다. 전체 평가셋 성능이나 의미 기반 검색의 우위는 아직 측정하지 않았다.

Python 3.10.7의 프로젝트 가상환경에서 NumPy 2.2.6, rank-bm25 0.2.2를 설치했다. [검색 테스트](tests/test_bm25.py) 4개와 기존 로더 테스트 4개가 모두 통과했다. 빈 입력·미등록 어휘·원문 ID 보존·동점 순서·top-k 경계를 확인했다. 실행 명령과 상위 결과는 EVALUATION의 B-2 항목에 기록한다. 검색 시간과 GPU 사용량은 측정하지 않았다.

원칙: 검색 기준선의 실패를 기록한 뒤 원인에 맞는 변수를 비교하며, 예제 하나를 맞추기 위해 즉시 설정을 조정하지 않는다.

---

## B-3. 채점 가능한 질문부터 검색 평가를 반복 실행할 수 있는가

첫 샘플에는 복수 정답 주석과 표·그림 근거가 섞여 있어, 모든 질문을 곧바로 하나의 정답 문단 집합으로 계산하면 평가 범위가 불명확해진다. [평가 모듈](src/eval/retrieval.py)은 정답 주석이 하나이고 답변 가능하며, 비어 있지 않은 모든 근거가 각각 본문 한 위치에 연결된 질문만 먼저 채점한다. 질문 ID를 하드코딩하지 않고 입력 데이터의 조건으로 선택한다. 복수 위치에 연결된 근거는 임의로 정답 위치를 정하지 않고 보류한다.

검색에는 본문과 질문만 전달한다. 채점은 상위 k개의 문단 ID와 정답 ID 집합을 비교하며 Hit·Recall·RR을 계산한다. 평균은 평가한 질문에 동일 가중치를 주고, 보류 질문은 0점으로 섞지 않는다. 복수 정답·미연결 근거 등 보류 이유를 질문별로 모두 출력하며, 평가 대상이 없으면 평균은 `null`이다. 연결된 근거만 남겨 Recall 분모를 축소하지 않는다.

**논문 `1909.00694`의 9개 질문 중 4개를 평가하고 5개를 보류했다. k=5에서 평균 Hit·Recall·MRR은 모두 0이었다.** 전체 순위로 별도 확인한 정답 근거 위치는 질문별로 9위, 30위, 11위, 6·20위였다. 실제 근거가 상위 5개 밖에 있어 이번 0점 결과와 일치했다. 전체 데이터셋 성능이나 개선 실험으로 해석하지 않는다. 조건·질문별 결과·보류 사유는 [EVALUATION](EVALUATION.md)의 B-3에 기록한다.

[평가 테스트](tests/test_retrieval_eval.py) 7개를 추가해 부분 Recall, 1부터 세는 순위, k 바깥 근거, 중복 ID, 보류 제외 평균, 다중 보류 이유, 빈 평가의 `null`, 잘못된 k를 확인했다. 기존 테스트를 포함한 15개가 통과했다. CPU 실행이며 검색 지연시간은 별도로 측정하지 않았다. 생성·표 처리·복수 정답 정책·nDCG·CI는 아직 구현하지 않았다.

원칙: 평가 범위를 먼저 명시하고, 채점할 수 없는 입력과 검색이 놓친 근거를 구분해 기록한다.

---

## B-4. 반환 개수를 늘리면 놓친 근거가 포함되는가

B-3에서 평가한 4개 질문을 그대로 사용하고 `top_k`만 5와 10으로 바꿔 비교했다. 두 실행의 평가·보류 질문이 같고, k=10 결과의 앞 5개 문단 ID와 점수가 k=5 결과와 같음을 확인했다. 코드와 검색 설정의 다른 부분은 변경하지 않았다.

**k=10에서 평균 Hit는 0.5, Recall은 0.375, MRR은 0.069444였다. k=5에서는 모두 0이었다.** 9위·6위 근거가 포함됐고 11위·30위 근거는 여전히 빠졌다. 원시 말뭉치 질문은 근거 2개 중 하나만 포함했다. 질문별 값과 집계 방식은 [EVALUATION](EVALUATION.md)의 B-4에 기록한다. 기준값이 0이라 상대 개선율은 계산하지 않는다.

질문당 반환 문단 수는 5개에서 10개로 늘었다. 생성 입력량·답변 품질·지연시간·비용은 이번 실험에서 측정하지 않았다. 단일 논문의 제한된 개발 질문 결과이며 k=10을 최종 설정으로 채택하지 않는다. 기존 코드의 기본값은 k=5로 유지한다.

원칙: 반환 범위를 넓혀 얻은 근거 포함률 상승과 검색 순위 자체의 개선을 구분한다.

---

## B-5. 같은 후보에서 리랭킹은 놓친 근거를 상위로 올리는가

B-4까지는 BM25 순위만 봤다. 여기서는 교차 인코더 리랭커를 붙여, BM25 후보 20개를 고정한 채 기존 순서 top-5와 리랭킹 후 top-5를 비교했다. 후보 수를 늘리는 효과와 순서를 바꾸는 효과를 섞지 않으려고 두 방식 모두 같은 후보 20개만 본다. 정답 근거는 리랭커 입력에 넣지 않고 채점에만 쓴다. 이미 학습된 가중치로 점수만 계산하는 추론이며 이 논문으로 미세조정하지 않는다.

[리랭커](src/retrieve/rerank.py)는 `BAAI/bge-reranker-v2-m3`를 로드해 (질문, 문단) 쌍의 관련도 logit을 계산하고 내림차순으로 재정렬한다. logit은 sigmoid 이전 값이라 음수가 정상이고, sigmoid는 단조 변환이라 정렬 순서를 바꾸지 않는다. 동점은 후보로 들어온 BM25 순서를 유지한다. [실험 스크립트](scripts/rerank_experiment.py)는 B-3의 평가 질문 선별을 재사용하려고 [평가 모듈](src/eval/retrieval.py)에서 `deferral_reasons`·`gold_paragraph_ids`를 헬퍼로 분리해 함께 쓴다. `evaluate_paper`의 동작과 기존 단위 테스트 15개 결과는 그대로다.

**평가 4개 질문·후보 20개·top-5·max_length 512에서, BM25 기존 top-5는 Hit·Recall·MRR 모두 0, 리랭킹 후 top-5는 Hit 0.25·Recall 0.25·MRR 0.125였다. 3개 중 1개(seed lexicon 크기 질문, gold BM25 11위 → 리랭킹 2위)만 top-5로 회복했다.** 후보 20개 중 max_length에 잘린 것은 0개였다. 이 값은 실제 리랭커 결과이며 이상적 상한(회복 가능 3문항 기준 0.75)이 아니고, 단일 논문·4문항이라 일반화가 아니다.

후보 20 안에 gold가 있었는데도 실패한 두 질문을 리랭커 순위·점수로 진단했다. 감독 학습 라벨 질문(gold `s0:p0`)은 gold가 리랭킹 14/20위로 깊이 묻힌 hard miss였고, 원시 말뭉치 크기 질문(gold `s11:p0`·`s11:p4`)은 6·7위로 top-5 문턱 바로 아래였던 near miss였다. 두 경우 모두 질문과 표면 어휘만 겹치는 문단이 gold 위로 올라왔다. 나머지 한 질문(gold BM25 30위)은 후보 20 밖이라 리랭킹으로는 회복할 수 없는 검색 단계 문제다. 질문별 순위·점수는 [EVALUATION](EVALUATION.md)의 B-5에 기록한다.

환경: torch 2.5.1+cu121, transformers 5.17.0, RTX 3080 GPU 추론. bge-reranker-v2-m3 가중치는 첫 실행에서 HF 캐시로 다운로드했다(약 2.3GB). 원시 결과는 `runs/rerank-first-paper-k20.json`에 저장하고 Git에서 제외한다. 생성 입력·답변 품질·지연시간은 측정하지 않았다.

원칙: 검색 recall(후보에 넣기)과 리랭킹(후보 안 재정렬)의 역할을 구분하고, 후보 안 실패도 near miss와 hard miss로 나눠 처방을 달리한다.

## B-6. dense 임베딩 검색은 BM25보다 나은가, hybrid는 이득이 있는가

B-5까지의 병목은 검색 recall이었다. BM25는 겹치는 단어로만 점수를 매겨, 같은 뜻을 다른 표현으로 쓴 근거를 놓친다(원시 데이터 학습 질문은 gold가 BM25 30위였다). [dense 검색](src/retrieve/embed.py)은 `BAAI/bge-m3`로 질문과 문단을 의미 벡터로 바꿔 코사인 유사도로 정렬한다. 편의 라이브러리 대신 raw transformers로 CLS 토큰을 뽑아 L2 정규화하는 최소 구현이며, 이미 학습된 가중치로 벡터만 계산하는 추론이다. torch 2.5.1이 보안 이슈로 `pytorch_model.bin`을 거부해 safetensors로 로드한다.

첫 논문(평가 4문항)에서 dense는 BM25가 30위로 묻은 근거를 5위로, 11위 근거를 1위로 끌어올려 top-5 평균 Hit을 0에서 0.5로 올렸다. 다만 gold가 질문과 같은 단어·숫자를 쓴 두 질문에서는 dense가 오히려 순위를 떨어뜨려, 어휘 매칭(BM25)과 의미 매칭(dense)이 상호보완적임을 봤다. 이를 합치려고 [hybrid](src/retrieve/hybrid.py)로 두 순위를 RRF(Reciprocal Rank Fusion, 1/(k+순위) 합, k=60)로 융합했으나, 첫 논문에서 hybrid Hit@5는 0.25로 dense 0.5보다 낮았다.

단일 논문 4문항은 결론을 내기에 표본이 너무 작다고 보고, [추출 스크립트](scripts/extract_papers.py)로 Qasper train 앞 10편을 뽑아 평가 질문 26개로 늘려 [세 방식을 마이크로 평균](scripts/hybrid_experiment.py)으로 비교했다. 원본(리스트형)을 로더가 읽는 컬럼형으로 변환하며, 추출본의 첫 논문이 기존 파일과 동일하게 로드됨을 검증했다.

**10편 26문항 마이크로 평균: BM25 Hit@5 0.385·Recall 0.346·MRR 0.237, dense 0.615·0.558·0.404, hybrid 0.423·0.404·0.304.** 26문항 중 Hit 개수는 BM25 10, dense 16, hybrid 11이다. 표본을 늘리자 세 가지가 드러났다. 첫째, 첫 논문에서 0이던 BM25가 0.385로, 그 논문이 BM25에 유난히 불리한 표본이었다(n=4로 결론 내지 않은 판단이 맞았다). 둘째, dense가 단일 최선이다. 셋째, 단순 RRF hybrid는 큰 표본에서도 dense보다 낮아 BM25와 dense 사이에 머문다.

hybrid가 이득이 없는 이유: RRF는 두 검색기가 둘 다 어느 정도 쓸 만할 때 이득인데, 여기선 BM25가 dense보다 확연히 약해 동등 가중 융합이 약한 신호로 강한 신호를 희석한다. "hybrid는 항상 둘보다 낫다"는 기대는 검색기 품질 격차가 클 때 깨진다. 따라서 W1 검색의 잠정 결론은 dense 단독이 최선이고 단순 RRF는 이득이 없다는 것이다. 질문별 순위·지표는 [EVALUATION](EVALUATION.md)의 B-6에 기록한다.

환경: torch 2.5.1+cu121, transformers 5.17.0, RTX 3080 GPU 추론. bge-m3 가중치는 첫 실행에서 HF 캐시로 다운로드했다(약 2.3GB). Qasper 원본과 원시 결과(`runs/`)는 용량·라이선스 때문에 Git에서 제외한다. 지연시간·인덱싱 시간은 측정하지 않았다.

원칙: 표본이 작을 때는 수치를 결론으로 굳히지 않는다(첫 논문 BM25 0 → 26문항 0.385). 검색기 품질 격차가 크면 동등 가중 융합이 손해일 수 있으므로, hybrid는 방식 간 성능이 비슷할 때 우선 검토한다.

## B-7. 1단계 검색기를 바꾸면 리랭커가 더 사는가 (dense→리랭커)

B-5의 리랭커는 1단계 검색기가 BM25였고, gold가 후보 20 밖(BM25 30위)이면 회복하지 못했다. 리랭커는 후보 집합 안에서만 순위를 바꾸므로 후보 밖 문단은 데려오지 못한다. 그래서 1단계 검색 recall이 리랭커 성능의 천장이다. 이를 수치로 확인하려고 [파이프라인 실험](scripts/pipeline_experiment.py)에서 같은 26문항(10편)에 세 방식을 비교했다. ⓐ dense 단독 top-5, ⓑ BM25 후보 20 → 리랭킹 top-5, ⓒ dense 후보 20 → 리랭킹 top-5. 정답 근거는 리랭커 입력에 넣지 않고 채점에만 쓴다.

**후보 20·top-5·마이크로 평균에서, dense 단독 Hit@5 0.615·Recall 0.558·MRR 0.404, BM25→리랭커 0.462·0.404·0.335, dense→리랭커 0.692·0.577·0.461이었다.** 세 가지가 드러난다. 첫째, dense→리랭커가 전 지표 최선으로 리랭킹이 dense 위에서 추가 이득을 낸다(Hit 0.615→0.692). 둘째, BM25→리랭커(0.462)가 dense 단독(0.615)보다도 낮다. 리랭킹을 해도 약한 후보는 좋은 후보 없이 못 이긴다. 셋째, 같은 리랭커인데 후보만 dense로 바꾸면 0.462에서 0.692로 오른다.

정리하면 리랭커의 성능 천장은 1단계 검색 recall이 정한다. gold가 후보 밖이면 재정렬로는 회복할 수 없고, BM25는 gold를 후보 밖이나 아래에 두는 경우가 많아 리랭킹으로도 dense를 따라잡지 못한다. W1 검색의 전체 그림은 BM25 0.385 → dense 0.615(큰 도약) → dense→리랭커 0.692(추가 보정)이며, dense 검색이 핵심 승부처이고 리랭킹은 좋은 후보 위에서 얹는 보정이다. 질문별 지표는 [EVALUATION](EVALUATION.md)의 B-7에 기록한다.

환경: torch 2.5.1+cu121, transformers 5.17.0, RTX 3080에서 bge-m3와 bge-reranker-v2-m3를 함께 로드해 추론. 원시 결과는 `runs/pipeline-10papers.json`에 저장하고 Git에서 제외한다. 지연시간·VRAM 사용량은 측정하지 않았다. 개발용 부분집합(26문항) 결과이며 held-out 최종 평가는 이후 단계다.

원칙: 파이프라인은 1단계 검색(recall)과 2단계 리랭킹(precision)의 역할을 나눠 본다. 리랭킹을 얹기 전에 1단계 recall을 먼저 끌어올린다.

## B-8. 답변 하네스 착수 — Answer-F1, 추출 베이스라인, 7B 생성, 검색 근거 손실

W1이 "맞는 근거를 찾았나"라면 W2는 "그 근거로 만든 답이 정확한가"다. 정확도는 [Answer-F1 채점기](src/eval/answer.py)로 재며 Qasper 공식 evaluator 로직을 따른다(정규화: 소문자화·문장부호 제거·관사 제거·공백 정리, 토큰 F1, 주석자 여러 명이면 최대 F1). gold 문자열 우선순위는 unanswerable → extractive_spans → free_form → yes_no다. 단위 테스트 12개로 정규화·F1·유형별 문자열·최대 집계를 검증했다.

검색 오류와 답 생성 오류를 섞지 않으려고 oracle(gold) 근거를 먼저 쓴다. [추출 베이스라인](scripts/answer_experiment.py)은 gold 근거 문단을 그대로 답으로 낸다. 10편 51문항에서 평균 Answer-F1 0.137이었다(extractive 0.166, abstractive 0.190, boolean·unanswerable 0). 완벽한 근거를 줘도 낮은 이유는 예측(문단 전체)이 정답 스팬보다 훨씬 길어 정밀도가 무너지기 때문이다. 즉 맞는 근거를 갖는 것과 정답을 쓰는 것은 다르며 답은 간결해야 한다.

[생성기](src/generate/qwen.py)는 `Qwen2.5-7B-Instruct`를 bitsandbytes 4-bit(NF4)로 로드해(VRAM 5.56GB) 근거로만 간결히 답하고 없으면 보류하도록 지시하며, 재현을 위해 그리디로 디코딩한다. 양자화 방식은 처음에 사전양자화 AWQ(다운로드·VRAM 유리)를 쓰려 했으나, autoawq가 torch를 2.3.1로 강제 다운그레이드해 기존 cu121 CUDA 빌드를 깨뜨려(설치 dry-run으로 확인) bitsandbytes 4-bit로 폴백했다. oracle 근거로 생성한 Answer-F1은 0.506이었고(추출 0.137의 3.7배), 프롬프트를 손봐 0.533까지 올렸다. 이때 여러 지시를 한꺼번에 넣은 버전은 boolean을 크게 고쳤지만 다른 유형을 깎아 순증이 작았고, 효과가 확인된 yes/no 지시 하나만 남긴 버전이 최선이었다. 과도 보류(근거가 있는데 보류) 8건 중 약 3건은 답이 표(FLOAT 캡션)에만 있어 본문 근거로는 풀 수 없었다.

마지막으로 oracle 근거를 [실제 검색 근거로 교체](scripts/answer_retrieved_experiment.py)했다(W1 최선인 dense 후보 20 → 리랭커 top-5, 검색기와 7B는 VRAM 제약으로 2단계 분리). 평균 Answer-F1은 0.533 → 0.416으로 검색 오류가 0.116을 깎았다. 손실 경로는 둘이다. 검색이 gold 문단을 top-5에 못 올리면 생성기가 좋아도 못 답하고(extractive 0.541→0.443), oracle에선 근거가 비어 정확히 보류하던 unanswerable이 검색은 답 없는 질문에도 top-5를 주는 탓에 노이즈를 보고 답을 지어낸다(1.000→0.500). 검색 품질이 답 품질의 천장이며, 이 0.116이 W1과 W2를 잇는다.

환경: torch 2.5.1+cu121, transformers 5.17.0, accelerate, bitsandbytes 0.50.2, RTX 3080. Qwen2.5-7B는 HF 캐시로 받았다(~15GB). 원시 결과는 `runs/answer-*.json`에 저장하고 Git에서 제외한다. Faithfulness(로컬 NLI) 축과 held-out 최종 평가는 이후 단계다. 유형별 개수는 예측이 가장 잘 맞는 reference로 분류돼 실행마다 흔들리므로 전체 평균을 헤드라인으로 본다.

원칙: 답 평가는 검색 오류와 생성 오류를 oracle로 분리해 각각 측정한다. 프롬프트는 변수를 하나씩 분리해 튜닝한다. 사전양자화 라이브러리는 torch 버전을 핀으로 걸 수 있어 설치 전 dry-run으로 의존성 변경을 확인한다.

## B-9. 충실성 축 — 답이 근거에 함의되는가(환각 측정)

Answer-F1은 reference와의 겹침만 본다. 답이 근거에서 실제로 나온 말인지(환각이 아닌지)는 별개 축이라 따로 잰다. [NLI 채점기](src/eval/nli.py)는 `MoritzLaurer/DeBERTa-v3-base-mnli-fever-anli`로 근거를 전제, 답을 가설로 두고 함의를 판정한다. 근거⊨답이면 충실(entailment), neutral이면 근거 밖에서 지어냄(환각), contradiction이면 근거와 반대다. 방향이 중요한 이유는 근거가 답보다 정보가 많아, 반대로 보면 짧은 답이 긴 근거를 함의할 수 없어 충실한 답도 neutral로 오판되기 때문이다. 라벨 순서는 config.id2label로 읽는다. NLI를 생성과 분리·재사용하려고 [실험 스크립트](scripts/nli_experiment.py)는 생성 결과 JSON에 저장된 근거·답을 읽어 채점하며, 생성 실험은 각 행에 근거를 저장하도록 보강했다. 보류(Unanswerable)는 주장을 안 한 것이라 충실성 채점에서 제외한다.

**51문항에서 oracle 근거 답은 충실율 0.778·환각율 0.222·모순율 0.056, 검색 근거 답은 충실율 0.615·환각율 0.385·모순율 0.103이었다.** 검색 노이즈는 정확도(Answer-F1 0.533→0.416)뿐 아니라 충실성(0.778→0.615, 환각 0.222→0.385)도 깎는다. 검색이 답과 무관한 문단을 주면 모델이 근거에 없는 말을 답에 넣어 함의가 깨진다. F1이 간접적으로만 보이던 환각을 직접 수치화한 것이다.

다만 oracle도 환각 0.222로 완전하지 않다. 완벽한 근거로도 22%가 엄격한 함의는 아닌데, 모델이 사전지식을 섞거나 서술형·수치 답을 NLI가 엄격히 못 잡거나 재구성 표현 때문이다. NLI는 근사 렌즈이므로 절대값보다 oracle 대비 검색의 상대 격차를 신뢰 신호로 본다. 두 축(정확도 F1, 충실성 NLI)을 함께 봐야 근거 없이 맞힌 답과 근거대로인데 표현이 달라 손해 본 답을 구분하는 진단이 완성된다. 환경은 sentencepiece(DeBERTa-v3 토크나이저) 추가 외 B-8과 같고, 원시 결과는 `runs/nli-*.json`에 저장해 Git에서 제외한다. 문장 단위 분해·인간 검증 상관·held-out 평가는 이후 단계다.

원칙: 정확도와 충실성은 직교하는 두 축이라 함께 재야 진단이 된다. 근사 지표는 절대값이 아니라 조건 간 상대 비교로 읽는다.

---

# Part C — 회고

## C-1. 배운 것
_(주제별 원칙 + 구체 사례, 초기 가정 vs 현실)_

## C-2. 남은 로드맵
_(위 체크박스 로드맵과 연동)_
