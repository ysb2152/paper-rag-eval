# --- Core RAG ---
# 로컬 GPU 임베딩/리랭커 (RTX 3080). torch는 CUDA 빌드로 별도 설치 권장:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
sentence-transformers>=3.0
FlagEmbedding>=1.2          # bge-m3 / bge-reranker-v2-m3
faiss-cpu>=1.8             # GPU 원하면 faiss-gpu (환경 맞으면)
rank-bm25>=0.2             # hybrid 검색용 BM25

# --- 채점 (무료 로컬) ---
transformers>=4.44         # 로컬 NLI(faithfulness) + 로컬 생성(Qwen2.5-7B)
accelerate>=0.30           # 4-bit/디바이스 매핑
bitsandbytes>=0.43         # 7B 4-bit 양자화 (Windows 휠 확인 필요)
evaluate>=0.4             # 지표 유틸(선택)

# --- 생성 · judge (선택: Claude 비교/검증용) ---
anthropic>=0.40

# --- 데이터 ---
datasets>=2.19            # Qasper 로드 (HF: allenai/qasper)
pandas>=2.2
numpy>=1.26

# --- 서빙 (Week 4) ---
fastapi>=0.110
uvicorn>=0.29

# --- 유틸 ---
pyyaml>=6.0               # ablation config
tqdm>=4.66
python-dotenv>=1.0
